const { PI: myPi, add: myAdd } = require('./arithemetic-operations');

console.log(`Sum of 10 and 20 is ${myAdd(10, 20)}`);
console.log(`Value of PI is ${myPi}`);

// console.log(`Subtract of 10 and 20 is ${subtract(10, 20)}`);